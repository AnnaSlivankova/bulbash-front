import { RootState } from '../../app/store'

// export const selectAuthIsSignin = (state: RootState) => state.auth.isSignin
// export const selectAuthIsSignup = (state: RootState) => state.auth.isSignup
